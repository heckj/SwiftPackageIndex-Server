apiVersion: v1
kind: Service
metadata:
  name: swiftpackageindex
  labels:
    app: webapp
spec:
  selector:
    app: webapp
  type: LoadBalancer
  ports:
    - name: webapp-tcp
      port: 8080
      targetPort: 8080
      protocol: TCP
