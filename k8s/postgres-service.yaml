# postgres-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  selector:
    app: postgres
  type: ClusterIP
  ports:
    - name: postgres-tcp
      port: 5432
      targetPort: 5432
      protocol: TCP